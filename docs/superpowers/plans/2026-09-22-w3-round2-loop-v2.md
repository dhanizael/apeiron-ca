# Plan — W3 round-2: loop v2 di semesta RICH (tuas level-tabel hidup kembali)

Tanggal: 2026-09-22. Log riset: 007. Branch: `w3-round2` → merge main.
Doktrin: genius-mind-doctrine + meta-quality Tier 3. TDD ketat.

## Latar (mengapa round-2 sah)

W3-NULL run-1..3 (log 005) teratribusi penuh: semesta juara POOR ~100%
capacity-bound → J tak peka terhadap mutasi tabel; intervensi di rezim jenuh
harus null. Log 006 menemukan lengan RICH (generator uniform [0, cap] per
entri, slack 61%) yang TIDAK runtuh di horizon 10⁶ (streak 0, lifetime 1000/
1000) — semesta yang TIDAK capacity-bound. Prediksi eksplisit log 006: di
semesta RICH, medan aliran tidak terjepit kapasitas → mutasi tabel punya
konsekuensi J → tuas level-tabel hidup. Round-2 menguji prediksi itu.

## Kontrak keberhasilan (beku sebelum eksekusi)

Requirement wajib (W):
- W1: Intervensi pada semesta RICH k=2 (champ seed 5700010, FNV 85eba35fbd113e83)
  diturunkan dari temuan Newton sendiri (entri longgar tersibuk), diukur dengan
  pasangan seed-sama (efek mutasi murni, nol derau seed), kontrol disjoint.
- W2: Kriteria W3v2 bivalen, ditetapkan SEBELUM run:
  W3v2-PASS ⟺ mean(ΔJ_fb) > mean(ΔJ_ctrl) dan semua ΔJ_fb ≥ 0, K=4 iterasi,
  diukur pada init_cap yang SAMA dengan rezim observasi (cap_lin, rezim linier).
- W3: Diagnosa atribusi wajib — setiap iterasi mencatat j_by_cap untuk
  melaporkan apakah efek hilang di rezim jenuh (cap tinggi) — replikasi
  pelajaran run-2 sebagai kontrol internal struktur.
- W4: Jika verdict NULL: bukan dipoles — wajib atribusi mekanistik per iterasi
  (pasangan seed-sama ⇒ derau seed=0; jika Δ fb = Δ ctrl eksak → cek disjoint
  & cap; jika Δ≈0 semua → ukur distribusi slack teramati & ketepatan tuas).
- W5: Mini mode deterministik byte-identik (standar harness).

Preferensi (bukan requirement):
- Harness Python + engine Rust yang sama (reproduksibilitas K1).
- File baru `experiments/m4/loop2.py`; v1 tidak diubah.

Instrumen: Python 3.14 (.venv), engine release build 650k, tabel champ di
`experiments/m1v2/result/_work/rule_H_RICH_5700010.bin`.

Batas eksternal: single-thread engine run (determinisme), horizon pengukuran
20.000 langkah window 512 (protokol loop v1), tanpa perubahan parameter beku
keluarga flow (k, n, generator) — intervensi HANYA nilai tabel, +1 per entri
terpilih (murni tuas level-tabel, sesuai gagang log 006).

## Kriteria penyelesaian (bivalen)

- K1: test_m4_loop_v2.py — mini e2e hijau + deterministik byte-identik 2 run.
- K2: FULL RUN loop v2 selesai dengan verdict bivalen (PASS/NULL) + atribusi
  mekanistik lengkap per iterasi di result.json.
- K3: Log 007 ditulis jujur (verdict apa adanya, koreksi bila ada overclaim).
- K4: Rust + Python test suite semua hijau; commit + merge main.

## Out of scope

- Klaim open-endedness (W3 bukan klaim OEE — bukti loop: temuan → intervensi
  → konsekuensi).
- M2 replikator; publish MAP.
- Perubahan protokol beku M0/K2.

## Pre-mortem (3 kegagalan paling mungkin + tandingan)

1. **J tak peka juga di RICH** (semesta RICH ternyata jenuh di cap_lin=6):
   tandingan — W3 atribusi mencatat j_by_cap; jika J identik semua mutasi,
   ukur slack teramati pada init_cap=6 untuk membuktikan mekanisme; lalu
   kontemplasi Langkah 4 (geser ukur di cap yang benar-benar linier, bukan
   mendiamkan).
2. **Δ fb ≈ Δ ctrl tapi ≠ 0** (keduanya naik karena +1 entri mana pun menaikkan
   J): itu bukan null murni — lapor sebagai "efek generik vs efek arah", dan
   uji kontrol besaran-sama tanpa arah memang disjoint. Kriteria bivalen tetap
   dari W2; atribusi membedakan mekanisme.
3. **Ketidakstabilan stat K=4** (variansi antar-iterasi menenggelamkan efek):
   K=4 mengikuti protokol v1 (beku); kalau sinyal margin sempit, lapor per
   iterasi semua Δ eksak — tidak ada agregasi yang menyembunyikan.

## Eksekusi

1. Test RED → implementasi GREEN (mini) → FULL RUN → verdict + atribusi.
2. Log 007 + now.md + memory; commit per checkpoint; merge main.
