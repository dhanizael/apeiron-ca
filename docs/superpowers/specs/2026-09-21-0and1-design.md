# 0and1 — Desain Sistem: Semesta Digital + Newton

**Status:** Draft desain — menunggu review user
**Tanggal:** 2026-09-21
**Klasifikasi:** Proyek riset ambisi tinggi. Doktrin GeniusMind aktif; disiplin epistemik penuh.

---

## 1. Visi

**North star:** membangun (1) **Semesta** — kosmos digital dari bit murni dengan fisika konservatif yang direkayasa agar kompleksitasnya tumbuh terus (open-endedness), dan (2) **Newton** — mesin penemu hukum yang menonton semesta itu dan menemukan hukum generatif/emergennya secara otomatis, diverifikasi eksak terhadap source code semesta — dalam satu loop tertutup.

**Pola jenius yang diikuti:** setiap tokoh yang menginspirasi proyek ini mengambil sesuatu yang abstrak dan memberinya representasi mekanis — Newton (gerak → kalkulus), Shannon (informasi → bit), Gödel (bukti → bilangan), Einstein (ruang-waktu → geometri), Bell (suara → sinyal). Yang masih abstrak hari ini: **open-endedness dan penemuan hukum** — keduanya belum punya "bit"-nya sendiri. 0and1 memberinya: **bit model Newton sebagai satuan open-endedness.**

**Trik epistemik inti:** semesta digital adalah satu-satunya semesta di mana kita memegang source code Tuhan. Hukum yang ditemukan Newton dapat diverifikasi eksak, bukan dinilai anekdotal.

---

## 2. Kontrak Keberhasilan (dibekukan)

### Requirements wajib (W)

| # | Requirement | Terukur oleh |
|---|---|---|
| W1 | Substrat bit-level dengan fisika konservatif, deterministik penuh, skala ≥10⁹ cell-update/detik di satu node | Benchmark + test determinisme |
| W2 | Newton memulihkan hukum semesta: objek/struktur, aturan mikro, dan hukum makro emergen — diverifikasi eksak vs source code | Akurasi pemulihan + protokol counterfactual law shift |
| W3 | Loop tertutup: temuan Newton mengumpan-balik ke evolusi substrat | Efek terukur pada lintasan evolusi |
| W4 | Metrik open-endedness operasional (growth-rate model Newton) + pelaporan dalam metrik komunitas | Kurva growth-rate + Bedau/Adami/Adams/kompresi |
| W5 | Reproducibilitas penuh: seed, manifest, harness | Re-run bit-identical |

### Kriteria penyelesaian (K) — biner

- **K1:** Dua run dengan seed sama menghasilkan snapshot bit-identical.
- **K2:** Throughput tercatat ≥ target W1 pada benchmark standar (dilaporkan apa adanya).
- **K3:** ≥1 aturan dengan partikel persisten DAN tanpa competitive exclusion dalam horizon H (M1). *Horizon H = jumlah langkah terkonfigurasi per eksperimen, nilai awal default 10⁶ langkah, dicatat di manifest dan dilaporkan.*
- **K4:** Newton memulihkan aturan mikro dengan akurasi setara/lebih baik dari baseline AutomataGPT (82% exact rule-matrix) di setting kita, dan memulihkan ≥1 hukum makro emergen yang terverifikasi (M3).
- **K5:** Kurva growth-rate model Newton terukur lintas ≥2 rezim fisika (M4).

### Preferences
- Rust untuk engine (performa + determinisme), Python hanya untuk analisis via file seam.
- Format snapshot biner + manifest JSON.

### Boundaries
- Satu node (i9-12900H, 20 thread, RAM tersedia ~6.8 GiB → snapshot **wajib streaming**, tidak boleh menahan semesta di memori).
- Tanpa GPU di fase ini (keputusan sadar, bukan down-scope: revisi bila benchmark menunjukkan kebutuhan).

### Out of scope (fase ini)
- Autopoiesis/self-repair tingkat lanjut (secara eksplisit belum pernah dicapai di self-reproducer artifisial mana pun — Sayama & Nehaniv 2024).
- Aplikasi ke data dunia nyata (fase setelah Newton tervalidasi di semesta sendiri).
- Klaim open-endedness tanpa batas tak terhingga (kita mengukur horizon yang dijalankan, bukan mengklaim keabadian).

---

## 3. Posisi Terhadap Prior Art (terverifikasi)

**Klaim novelty (bentuk final, dibatasi bukti):** *Tidak ditemukan dalam sumber-sumber primer yang dibuka* satu pun sistem yang menempatkan mesin pemulih hukum di atas substrat yang **ruang hukumnya bergerak**, memverifikasi terhadap **ground truth yang bergerak**, atau **menutup umpan balik** temuan ke substrat. Syarat (iii) dan (iv) kosong; (i) dan (ii) ada terpisah.

| Karya | Apa yang sudah dilakukan | Kenapa bukan kopling kita |
|---|---|---|
| LOGOS-CA (2602.00036) | Ruang hukum terbuka (aturan = string bahasa alami dieksekusi LLM) | Tanpa penemu hukum, tanpa verifikasi, tanpa feedback |
| SciExplorer (2509.24978, PRX) | Penemu hukum otomatis (persamaan gerak, Hamiltonian) | Dunia statis |
| AutomataGPT (2506.17333) | Pemulihan aturan mikro CA, 82% exact match | Aturan tetap, hanya 100 dari 2^18, biner 2D; menemukan aturan **mikro**, bukan hukum emergen makro |
| ASAL (2412.17799) / ASAL++ | Pencarian substrat ALife otomatis via FM | Menemukan konfigurasi/parameter, bukan hukum; tanpa ground truth |
| Flow-Lenia AI scientist (2505.15998) | Eksplorasi ruang parameter dengan metrik kompleksitas | Menemukan wilayah parameter; eksplisit bukan hukum; tanpa ground truth |
| Hughes et al. 2024 (2406.04268) | Definisi: open-ended iff novel + learnable (observer-dependent) | Definisi tanpa sistem; tanpa ground truth |
| Bedau 1998 / Adami 2000 / Adams 2017 | Metrik aktivitas evolusioner / physical complexity / unbounded evolution | Metrik tanpa loop penemuan hukum |
| Chan 2023 (2304.05639) | Lenia skala besar: OEE belum tercapai; goo/LEP dominance | Bukti kegagalan, bukan solusi |

**Batas kejujuran:** scan terarah ~60 sumber primer, bukan systematic review. arXiv API hanya metadata; Scholar/DDG sebagian diblokir; venue ALIFE/GECCO/journal belum disapu. Bahasa klaim mengikuti batas ini. Satu snippet arXiv (2604.01684) terbukti halusinasi saat diverifikasi — contoh mengapa disiplin sumber primer wajib.

---

## 4. Arsitektur

Tiga lapis, satu sistem:

```
┌──────────────────────────────────────────────┐
│ SEMESTA — engine substrat (Rust)             │
│ CA konservatif k-bit, bit-packed,            │
│ deterministik, multithreaded                 │
│ → bitstream + snapshot + manifest            │
└──────────────────┬───────────────────────────┘
                   │ file seam (snapshot biner + manifest JSON)
                   ▼
┌──────────────────────────────────────────────┐
│ NEWTON — penemu hukum (Rust/Python)          │
│ Tahap A: temukan objek/struktur (partikel,   │
│          domain, defect)                     │
│ Tahap B: temukan dinamika (MDL + proposal    │
│          terpelajar + verifier eksak)        │
│ → model generatif + akurasi vs ground truth  │
└──────────────────┬───────────────────────────┘
                   │ model size (bit) per waktu semesta
                   ▼
┌──────────────────────────────────────────────┐
│ INSTRUMEN & TEORI (Python)                   │
│ T1 growth-rate model Newton (definisi OEE)   │
│ T2 metrik komunitas (Bedau/Adami/Adams/komp) │
│ T3 oracle: akurasi vs source code +          │
│    counterfactual shift + efek feedback      │
└──────────────────────────────────────────────┘
```

Loop tertutup (M4): temuan Newton → parameter/aturan evolusi substrat berikutnya → semesta baru → Newton lagi.

---

## 5. Substrat (Semesta)

**Kelas:** number-conserving cellular automata (NCCA) k-bit di lattice (dimensi & radius dapat dikonfigurasi), update sinkron dan asinkron, boundary periodik.

**Dasar terverifikasi:** Fredkin–Toffoli conservative logic (komputasi universal kompatibel dengan konservasi; billiard ball model = gate universal dari tumbukan); Margolus BBMCA (reversibel, konservatif, universal; hukum konservasi lokal tidak bergantung pada reversibilitas aturan); flow diagram NCCA mendeskripsikan partikel via barisan kecepatan. → partikel, tumbukan, gate universal **native**, bukan diprogram.

**Peringatan desain (dari scan):** konservasi = **infrastruktur**, bukan open-endedness. Jangan dicampuradukkan.

**Parameter keluarga aturan:** k (bit per sel), radius, mode update, derajat konservasi, knob kekacauan tipe-λ, komponen bergantung-state (Adams 2017: satu-satunya mekanisme yang menghasilkan open-ended evolution secara scalable dalam uji mereka).

**Engine (M0):**
- Rust, bit-packed `u64`, multithreaded (20 thread), integer/bit ops murni — **tanpa float di substrat** → determinisme bit-identical by construction.
- Snapshot biner berversi + manifest JSON (seed, rule-id, size, step, hash).
- Streaming snapshot (RAM constraint); ring buffer frame terbaru.
- Benchmark harness: cell-update/detik, dilaporkan apa adanya.

---

## 6. Newton

**Tahap A — struktur dulu** (pelajaran Crutchfield: filter domain/partikel dibangun tangan sebelum persamaan gerak bisa diturunkan → kita otomatiskan tahap ini): deteksi partikel/domain/defect dari bitstream tanpa asumsi bentuk.

**Tahap B — dinamika** (teknik dari bukti head-to-head): hybrid tiga komponen —
1. **Parsimoni eksplisit** (MDL/posterior) untuk seleksi model — bukan akurasi saja.
2. **Proposal terpelajar** (neural-guided decomposition ala AI Feynman yang menaikkan 71%→100%) untuk pencarian.
3. **Verifier eksak** sebagai gerbang penerimaan — mencegah konfabulasi (pelajaran FunSearch).

**Larangan teknis:** GP/EA murni sebagai tulang punggung (kalah di semua head-to-head); sparse-library (SINDy) untuk hukum makro (hukum emergen kemungkinan tidak ada di pustaka manusia; library tumbuh faktorial terhadap dimensi); asumsi toleransi noise (semesta kita bebas noise — itu keunggulan, bukan alasan malas).

**Protokol verifikasi:** (a) akurasi pemulihan vs source code; (b) **counterfactual law shift** (pola NewtonBench: geser hukum mikro → semesta baru → hukum makro tersembunyi) sebagai tes ketahanan hafalan sekaligus verifikasi eksak.

**Anggaran realistis:** biaya pencarian eksponensial terhadap kompleksitas hukum dan kuadratik terhadap dimensi (Schmidt–Lipson: 30 jam untuk double pendulum). Desain harus mengasumsikan pencarian terbatas — bukan brute force.

---

## 7. Metrik & Teori

**Tiang 1 — definisi operasional (kontribusi kita):** semesta disebut open-ended bila **ukuran model terbaik Newton tumbuh persisten** — bit model per satuan waktu semesta. Mengadopsi + mengkuantifikasi Hughes et al. 2024 ("novel + learnable", observer-dependent). Ground truth membuatnya terverifikasi, bukan anekdotal.

**Tiang 2 — komparabilitas:** Bedau evolutionary activity (class 1/2/3), Adami physical complexity (bit mutual information genom–lingkungan), Adams unbounded evolution (non-repetisi vs waktu rekurensi Poincaré), complexity profile berbasis kompresi (zstd). Hasil wajib dilaporkan dalam bahasa ini.

**Tiang 3 — oracle (unik):** akurasi pemulihan vs source code; counterfactual shift; efek feedback (perubahan lintasan evolusi akibat temuan Newton).

---

## 8. Desain-sekitar Kegagalan

**Mode kegagalan universal: competitive exclusion.** Semua sistem spasial dalam scan berakhir dominasi satu/beberapa spesies (Lenia: goo/LEP; evoloop: regresi ukuran; Tierra: 8 kelas terkecil; Echo: bounded class 2).

Mitigasi ber-bukti (belum terbukti untuk horizon tak terbatas — jujur): mass conservation + energy constraints; aturan bergantung-state; lingkungan dinamis.

**Tiga asumsi terlarang** (dibantah/tidak didukung sumber primer):
1. Konstruktor universal ⇒ kompleksitas tumbuh (von Neumann 1966 belum pernah dipakai untuk evolusi Darwinian).
2. Konservasi/reversibilitas ⇒ open-endedness.
3. Keanekaragaman bentuk ⇒ evolvability (Lenia >400 spesies, tetap konvergen).

---

## 9. Milestone

| # | Isi | Kriteria biner |
|---|---|---|
| M0 | Engine + repro harness + benchmark | K1 determinisme, K2 throughput |
| M1 | Physics design search | K3: ≥1 aturan partikel persisten + tanpa competitive exclusion dalam horizon H |
| M2 | Replikator dari primitif substrat | Replikasi terdeteksi tanpa diprogram; fidelity terukur |
| M3 | Newton menemukan hukum semesta kita | K4: pemulihan aturan mikro ≥ baseline + ≥1 hukum makro emergen terverifikasi |
| M4 | Loop tertutup + teori pengukuran | K5: kurva growth-rate lintas ≥2 rezim + efek feedback terukur |

---

## 10. Engineering & Reproducibilitas

- **Layout repo:** `engine/` (Rust core + CLI + benchmark), `analysis/` (Python: metrik, Newton tahap B, plot), `experiments/` (manifest + hasil), `docs/` (spec, research).
- **Determinisme:** tanpa float di substrat; urutan iterasi tetap; RNG seeded eksplisit; hash snapshot per manifest.
- **Setiap klaim eksperimen wajib punya:** manifest (seed, versi kode, parameter) + perintah reproduksi.
- **Git:** repo diinisialisasi; checkpoint commit per milestone.

---

## 11. Risiko Terbuka & Status Epistemik

| # | Risiko | Status |
|---|---|---|
| R1 | Pertumbuhan model persisten mungkin tidak tercapai di sistem finit mana pun | Pertanyaan terbuka yang kita kerjakan — bukan klaim |
| R2 | Competitive exclusion mungkin mengalahkan semua mitigasi yang diketahui | Bukti kuat bahwa ini sulit; mitigasi kita belum terbukti |
| R3 | Newton mungkin tidak skalabel ke hukum makro emergen | Tanpa preseden (AutomataGPT hanya aturan mikro); risiko desain |
| R4 | Klaim novelty dibatasi cakupan retrieval | Terverifikasi dalam batas sumber yang dibuka; wording mengikuti |

**Status klaim:** semua referensi di §3 berstatus VERIFIED (sumber primer dibuka; catatan lengkap di `docs/research/prior-art/`). Klaim arsitektur berstatus hipotesis desain sampai diuji di M0–M4.

---

## 12. Referensi Inti

Prior art wajib sitasi: LOGOS-CA (2602.00036), SciExplorer (2509.24978), AutomataGPT (2506.17333), ASAL (2412.17799), ASAL++ (2509.22447), Leniabreeder (2406.04235), SR-Scientist (2510.11661), DGM (2505.22954), Hughes et al. (2406.04268), Chan (2304.05639), Sayama & Nehaniv (2402.03961), Bedau et al. (1998), Adami et al. (2000), Adams et al. (1607.01750), Fredkin & Toffoli (1982), Margolus (BBMCA), NCCA (adap-org/9905004), Fontana & Buss (SFI WP 93-09-055), Schmidt & Lipson (2009), AI Feynman (1905.11481), Bayesian machine scientist (2004.12157), SINDy (1509.03580), DreamCoder (2006.08381), FunSearch (Nature 2023), AlphaEvolve (2506.13131), NewtonBench (2510.07172), DiscoverPhysics (2605.26087), AI Physicist (1810.10525), POET (1901.01753), Enhanced POET (2003.08536), XLand (2107.12808), Voyager (2305.16291), AI Scientist (2408.06292), ADAS (2408.08435).
